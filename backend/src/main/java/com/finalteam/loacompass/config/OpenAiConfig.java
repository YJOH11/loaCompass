package com.finalteam.loacompass.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class OpenAiConfig {
    // OpenAiService는 이제 직접 생성자에서 생성되므로 여기서 빈을 등록할 필요가 없습니다.
}
